apiVersion: v1
kind: Service
metadata:
  name: transfer
  namespace: transfer
spec:
  type: ClusterIP  # Internal only, accessed via API Gateway
  selector:
    app: transfer
  ports:
  - port: 8080
    targetPort: 8080
