# PostgreSQL for Transfer Service
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-transfer-pvc
  namespace: infra
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-transfer-config
  namespace: infra
data:
  POSTGRES_DB: transferdb
  POSTGRES_USER: transferservice
  POSTGRES_PASSWORD: transferpass
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-transfer
  namespace: infra
  labels:
    app: postgres-transfer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-transfer
  template:
    metadata:
      labels:
        app: postgres-transfer
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        envFrom:
        - configMapRef:
            name: postgres-transfer-config
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-transfer-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-transfer
  namespace: infra
spec:
  selector:
    app: postgres-transfer
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
