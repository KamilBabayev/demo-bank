# PostgreSQL for Card Service
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-card-pvc
  namespace: infra
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-card-config
  namespace: infra
data:
  POSTGRES_DB: carddb
  POSTGRES_USER: cardservice
  POSTGRES_PASSWORD: cardpass
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-card
  namespace: infra
  labels:
    app: postgres-card
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-card
  template:
    metadata:
      labels:
        app: postgres-card
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        envFrom:
        - configMapRef:
            name: postgres-card-config
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-card-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-card
  namespace: infra
spec:
  selector:
    app: postgres-card
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
