apiVersion: v1
kind: Service
metadata:
  name: card
  namespace: card
spec:
  type: ClusterIP  # Internal only, accessed via API Gateway
  selector:
    app: card
  ports:
  - port: 8080
    targetPort: 8080
